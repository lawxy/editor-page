import React from 'react';
import { type ContainerProps } from './types';
declare function MonacoContainer({ style, isEditorReady, _ref, className, wrapperProps, }: ContainerProps): React.JSX.Element;
export default MonacoContainer;
