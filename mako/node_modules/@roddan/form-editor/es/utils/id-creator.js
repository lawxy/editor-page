export function getRandomString() {
  return Math.random().toString(32).slice(2);
}

/**
 * 生成id
 */
export function idCreator(prefix) {
  prefix = prefix || 'el';
  return "".concat(prefix, "-").concat(getRandomString());
}