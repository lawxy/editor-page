import store from "../store";
export var useCurrent = function useCurrent(type) {
  if (type === 'element') {
    return {
      current: store.selectedElement,
      setProp: store.setSelectedProp
    };
  }
  return {
    current: store.formAttrs,
    setProp: store.setFormAttr
  };
};