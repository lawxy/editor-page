import React from 'react';
import type { IEventTarget } from "../../../types";
declare const Validate: React.FC<{
    onChange: (v: Omit<IEventTarget, 'id' | 'sourceId'>) => void;
    eventTarget?: IEventTarget;
}>;
export default Validate;
