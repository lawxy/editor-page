import React from 'react';
import { Button } from 'antd';
import { observer } from 'mobx-react-lite';
import { useRegisterEvents } from "../../hooks";
import { ElementLayout } from "../../components";
import { EEventAction } from "../../types";
var RenderButtonContent = function RenderButtonContent(_ref) {
  var element = _ref.element;
  var _useRegisterEvents = useRegisterEvents(element),
    eventFunctions = _useRegisterEvents.eventFunctions;
  var btnText = element.btnText;
  return /*#__PURE__*/React.createElement(ElementLayout, {
    element: element
  }, /*#__PURE__*/React.createElement(Button, {
    onClick: function onClick() {
      var _eventFunctions$EEven;
      (_eventFunctions$EEven = eventFunctions[EEventAction.ON_CLICK]) === null || _eventFunctions$EEven === void 0 || _eventFunctions$EEven.call(eventFunctions);
    }
  }, btnText));
};
export var RenderButton = observer(RenderButtonContent);