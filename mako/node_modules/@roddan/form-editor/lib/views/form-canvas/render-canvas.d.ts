import React from 'react';
import type { TMode } from "../../types";
import './style.less';
export interface IEditorCanvasProp {
    /**
     * 表单模式
     */
    mode: TMode;
    /**
     * 表单操作按钮
     */
    actions?: React.ReactNode;
}
declare const _default: React.FunctionComponent<React.PropsWithChildren<IEditorCanvasProp>>;
export default _default;
