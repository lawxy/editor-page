"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertCSStoReactStyle = convertCSStoReactStyle;
function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }


function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }
var ColAttrs = ['marginLeft', 'marginRight', 'marginTop', 'marginBottom'];
function convertCSStoReactStyle(cssString, filterColAttrs) {
  var style = {};
  // const cssWithoutSelector = cssString.replace(/^[^{]*{\s*|\s*}[^}]*$/g, '');
  var cssWithoutSelector = cssString.substring(cssString.indexOf('{') + 1, cssString.lastIndexOf('}')).trim();
  var rules = cssWithoutSelector.split(';');
  rules.forEach(function (rule) {
    if (rule.trim()) {
      var _rule$split$map = rule.split(':').map(function (item) {
          return item.trim();
        }),
        _rule$split$map2 = _slicedToArray(_rule$split$map, 2),
        property = _rule$split$map2[0],
        value = _rule$split$map2[1];

      // Convert property from kebab-case to camelCase
      var camelCaseProperty = property.replace(/-([a-z])/g, function (g) {
        return g[1].toUpperCase();
      });
      if (filterColAttrs && ColAttrs.includes(camelCaseProperty)) return;
      style[camelCaseProperty] = value;
    }
  });
  return style;
}