import { EventEmitter } from "../utils";
import { IBaseElement, TCustomEvents } from "../types";
declare class EventStore {
    constructor();
    emitter: EventEmitter;
    eventMap: Map<any, any>;
    addRelation(targetId: string, sourceId: string): void;
    getSetsFromId(targetId: string): Set<string>[];
    deleteId(targetId: string): Promise<any>;
    iterate(events?: TCustomEvents): void;
    iterateEl(el: IBaseElement): void;
}
declare const _default: EventStore;
export default _default;
