"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _mobx = require("mobx");
var _utils = require("../utils");
var _ = _interopRequireDefault(require("."));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (!it) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = it.call(o); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it.return != null) it.return(); } finally { if (didErr) throw err; } } }; }
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : String(i); }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } /**
 * 存储事件源和事件目标间的映射关系, 用于实时通知事件失效
 */
var EventStore = /*#__PURE__*/function () {
  function EventStore() {
    _classCallCheck(this, EventStore);
    _defineProperty(this, "emitter", new _utils.EventEmitter());
    _defineProperty(this, "eventMap", new Map());
    (0, _mobx.makeAutoObservable)(this);
  }
  _createClass(EventStore, [{
    key: "addRelation",
    value: function addRelation(targetId, sourceId) {
      var _this$eventMap$get;
      var set = (_this$eventMap$get = this.eventMap.get(targetId)) !== null && _this$eventMap$get !== void 0 ? _this$eventMap$get : new Set();
      set.add(sourceId);
      this.eventMap.set(targetId, set);
    }
  }, {
    key: "getSetsFromId",
    value: function getSetsFromId(targetId) {
      var _this = this;
      var sets = [];
      var set = this.eventMap.get(targetId);
      if (set) sets.push(set);
      var targetElement = _.default.getElement(targetId);
      _.default.dfsEl(targetElement, function (child) {
        sets.push.apply(sets, _toConsumableArray(_this.getSetsFromId(child.id)));
      });

      // 容器组件要判断内部子组件
      // if (targetElement && targetElement?.children?.length) {
      //   targetElement.children.forEach((child) => {
      //     sets.push(...this.getSetsFromId(child.id!));
      //   });
      // }
      return sets;
    }
  }, {
    key: "deleteId",
    value: function deleteId(targetId) {
      var sets = this.getSetsFromId(targetId);
      if (!sets.length) return Promise.resolve(true);
      var exist = false;
      sets.forEach(function (set) {
        // @ts-ignore
        var _iterator = _createForOfIteratorHelper(set.keys()),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var sourceId = _step.value;
            if (_.default.getElement(sourceId) || _.default.getService(sourceId)) {
              exist = true;
            } else {
              set.delete(sourceId);
            }
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      });
      if (!exist) return Promise.resolve(true);
      var map = this.eventMap;
      return (0, _utils.ModalPromisify)({
        // title: `${exist ? '此组件或服务有事件关联, ' : ''}确认删除?`,
        title: '此组件(含内部组件)或服务有事件关联, 确认删除?',
        onOk: function onOk() {
          if (exist) {
            map.delete(targetId);
          }
        }
      });
    }
  }, {
    key: "iterate",
    value: function iterate() {
      var _this2 = this;
      var events = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
      events.forEach(function (event) {
        var eventTargets = event.eventTargets;
        eventTargets === null || eventTargets === void 0 || eventTargets.forEach(function (target) {
          var targetElementId = target.targetElementId,
            targetServiceId = target.targetServiceId,
            sourceId = target.sourceId;
          if (targetElementId) _this2.addRelation(targetElementId, sourceId);
          if (targetServiceId) _this2.addRelation(targetServiceId, sourceId);
        });
      });
    }
  }, {
    key: "iterateEl",
    value: function iterateEl(el) {
      var _this3 = this;
      var events = el.events;
      this.iterate(events);
      _.default.dfsEl(el, function (child) {
        _this3.iterateEl(child);
      });
    }
  }]);
  return EventStore;
}();
var _default = exports.default = new EventStore();