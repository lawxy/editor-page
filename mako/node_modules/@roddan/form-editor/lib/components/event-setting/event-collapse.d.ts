import { PropsWithChildren, type FC } from 'react';
import { type TCustomEvent } from "../../types";
export declare const EventCollapse: FC<{
    onDelete: (id: string) => void;
    collopaseItems: Array<{
        label: string;
        events: TCustomEvent[];
    }>;
    EditComponent: FC<PropsWithChildren<{
        evt: TCustomEvent;
    }>>;
}>;
